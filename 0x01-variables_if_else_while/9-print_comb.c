#include <stdio.h>
/**
 * main - the program starts here
 *
 * Return: Always (0) success
 */
int main (void)
{
	char lett;

	for(lett = '0'; lett <= '9'; lett++)
	{
		putchar (c);

		if (c != 9)
		{
			putchar(',');
			putchar(' ');
		}
	}


	putchar('\n');

	return (0);
}
